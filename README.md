# RainCast
RainCast is a Python-based machine learning project that predicts whether it will rain tomorrow in Australia using a Gaussian Naive Bayes classifier trained on a cleaned subset of the WeatherAUS dataset.
